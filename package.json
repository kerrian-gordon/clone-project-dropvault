{
  "name": "dropvault",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start:api": "node apps/api/src/start.js",
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build",
    "test": "node --test apps/api/test/*.test.js && npm --prefix apps/web test"
  }
}
